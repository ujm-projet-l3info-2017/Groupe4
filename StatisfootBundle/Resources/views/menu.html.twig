 <div class="menu_list">
	<div class="el_container first_el">
		<div class="el">COMPETITIONS</div>
		<ul id="pop_menu_competitions">
			{% for compet in competition %}

			<li> 
				<a href="{{ path('statisfoot_compet', {'id': compet.id}) }}">
				{{compet.nomCompet}}
				</a>
			</li>
			{% endfor%}
	  	</ul>
	</div>
</div>
<div class="menu_list">
		<div class="el_container">
			<div class="el">CLUBS</div>
			<ul id="pop_menu_clubs">
		  		{% for club in lesClubs %}
				<li> 
					<a href="{{ path('statisfoot_club', {'id': club.id}) }}">
					{{club.nomClub}}
					</a>
				</li>
				{% endfor%}
	  		</ul>
		</div>
</div>