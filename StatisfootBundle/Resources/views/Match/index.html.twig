<!DOCTYPE html>

<html>

<head>

  <meta charset="utf-8">

  <title>STATIS-FOOT</title>
</head>


<body>
<ul>
	{%for match_equipe in listeMatch%}
	<li>
		{%for match in match_equipe%}
		{{match.match.id}}
		{{match.match.lieu}}
		{{match.match.dateMatch|date('d/m/Y')}}</br>
		{{match.equipe.nom}} VS
		{%endfor%}
		
	</li>
	{%endfor%}
</ul>
</body>
</html>